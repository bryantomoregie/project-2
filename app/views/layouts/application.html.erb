<!DOCTYPE html>
<html>
  <head>
    <title>AppName</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <div class="header">
  
  <div class="header-left">
    <a class="active" href="/homepage">Home</a>
    <a href="/users/login">Log-in</a>
    <a href="/about">About</a>
  </div>
</div>
    <div class="nav-bar">
      <li id="n1"><a class="nav1" href="/homepage">Homepage</a></li>
      <li id="n2"><a class="nav2" href="/users/login">Login</a></li>
      <li id="n3"><a class="nav3" href="/users/new">Sign Up</a></li>
      <li id="n4"><a class="nav4" href="/about">About</a></li>
      <% if (session[:user_id]) %>
      <% user = User.find(session[:user_id]) %>
            <li id="n5"><%= link_to user.user_name, user %></li>
            <li id="n6"><%= link_to "Log Out", "/users/logout" %></li>
            <%end%>

    </div>
  </head>
<style>
.nav-bar{
  overflow: hidden;
  background-color: white;
  height: 24px;

}
#n1, #n2, #n3, #n4, #n5, #n6
{
  display: inline;
  padding: 10px;
}
a{
    text-decoration: none;
}
body{
  margin: 0;
}
/* Style the header with a grey background and some padding */
.header {
  overflow: hidden;
  background-color: #f1f1f1;
  padding: 20px 10px;
}

/* Style the header links */
.header a {
  float: left;
  color: black;
  text-align: center;
  padding: 12px;
  text-decoration: none;
  font-size: 18px;
  line-height: 25px;
  border-radius: 4px;
}

/* Change the background color on mouse-over */
.header a:hover {
  background-color: black;
  color: black;
}

/* Style the active/current link*/
.header a.active {
  background-color: dodgerblue;
  color: black;
}

/* Float the link section to the right */
.header-left {
  float: left;
}

/* Add media queries for responsiveness - when the screen is 500px wide or less, stack the links on top of each other */
@media screen and (max-width: 500px) {
  .header a {
    float: none;
    display: block;
    text-align: left;
  }
  .header-right {
    float: none;
  }
}
</style>
  <body>
    <%= yield %>
  </body>
</html>
